<%= render partial: 'splash', locals: { image: "baseball-wide" }%>

<%= render partial: 'main', locals: {
                                      league: "MLB",
                                      cbs_articles: @cbs_articles,
                                      espn_articles: @espn_articles,
                                      yahoo_articles: @yahoo_articles
                                    } %>
<%= render partial: 'mlb_footer' %>

<script>
  $(document).ready(function(){
    var html = ""
    $.ajax({
      url: "http://mlb.com/gen/<%= @team %>/news/headlines.json",
      dataType: "JSON"
    }).done(function(response){
      var j = 9
      if (response.members.length < i){
        j = response.members.length
      }

      html += '<div class="card-deck-wrapper"><div class="card-deck">'
      html += '<div class="card"><br>'
      html += '<img class="card-img-top img-fluid img-limit center-block" src=<%=@logo%>>'
      html += '<div class="card-block"><h4 class="card-header">Headlines</h4>'
      for(var i = 0; i < 5; i++ ){
        html += '<p class="card-text"><a target="_blank" href="' +   response.members[i].url + '">' +   response.members[i].headline + '</a></p>'
      }
      html += '</div></div>'
      html += '<div class="card"><br>'
      html += '<img class="card-img-top img-fluid img-limit center-block" src=<%=@logo%>>'
      html += '<div class="card-block"><h4 class="card-header">Headlines</h4>'
      for(var k = 4; k < j; k++ ){
        html += '<p class="card-text"><a target="_blank" href="' +  response.members[k].url + '">' +   response.members[k].headline + '</a></p>'
      }
      html += '</div></div></div></div>'
      $('div .col-lg-12').append(html)
    })
  })
</script>
